# Game of Thrones Characters

This is a small project to hold the CreateFuture take home assessement.

## Adding Secrets.xcconfig
The Secrets.xcconfig has been removed as this contains the api bearer token. To add this back in, follow these steps.
- Create the file Secrets.xcconfig within GOTCharacters/Config
- Add `API_BEARER_TOKEN = your token` to the file